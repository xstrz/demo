<h1>Home</h1>

<% @posts.each do |post| %>
	Post: <%= post.content %> 
	| <%= link_to "show", post %> 
	<% if user_signed_in? %>
		| <%= link_to "edit", edit_post_path(post) %>
		| <%= link_to "delete", post, :method	=> :delete %>
	<% end %>
	<br />
<% end %>